name = "clincoo"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# D1 akun 62cd (proxy clinqoo.pages.dev) — JANGAN diubah ke D1 be2 (akun lain, deploy akan gagal).
# Deploy proxy: wrangler pages deploy . --project-name=clincoo --branch=main --config wrangler-proxy.toml
[[d1_databases]]
binding = "DB"
database_name = "clincoo-db"
database_id = "2c3ef715-b117-4aa6-8108-0948c9646132"
